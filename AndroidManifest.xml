<?xml version="1.0" encoding="utf-8"?>
<manifest package="my.coupon.manager"
    android:versionCode="1"
    android:versionName="1.0" xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-sdk android:minSdkVersion="8" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" >
        <activity
            android:name=".CouponManagerActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignUp" android:label="@string/txt_signup"></activity>
        <activity android:name=".MainScreen" android:label="@string/app_name"></activity>
        <activity android:name=".CouponList" android:label="@string/app_name"></activity>
        <activity android:name=".CouponDetails" android:label="@string/app_name"></activity>
    </application>

</manifest>